[Service]
# Fedora's hardened vendor unit uses its own `seat` group. Use the project
# group that is also retained inside each rootless desktop container.
ExecStart=
ExecStart=/usr/bin/seatd -g bootsybox-seat
